# Telegram Reposter

Инструмент для автоматического репостинга постов из Telegram-каналов с перефразированием текста через нейросети.

## 📋 Описание

Cканирует указанные Telegram-каналы, фильтрует посты по популярности (просмотры, реакции, комментарии), перефразирует текст с помощью нейросетей (io.net) и отправляет в целевой канал. Поддерживает медиа-посты (фото, видео, документы, гифки).

## ✨ Возможности

- ✅ Автоматический репост из нескольких каналов
- ✅ Фильтрация по популярности (просмотры/реакции/комментарии)
- ✅ Перефразирование текста через 15+ моделей нейросетей
- ✅ Поддержка медиа-постов (фото, видео, документы, анимации)
- ✅ Защита от дубликатов (история отправленных постов)
- ✅ Health-check моделей нейросетей
- ✅ Статистика выполнения
- ✅ Настраиваемые задержки между постами
- ✅ Rich-логирование с цветами и эмодзи

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Используя uv
uv sync

# Или используя pip
pip install -r requirements.txt
```

### 2. Настройка config.ini

Отредактируйте файл `config.ini`:

### 3. Получение ID каналов

1. Перешлите любое сообщение из нужного канала боту **@JsonDumpBot**
2. В ответе найдите ключ `forward_origin -> chat -> id`
3. Скопируйте числовой ID (включая минус для каналов)

### 4. Запуск

```bash
python main.py
```

При первом запуске потребуется ввести номер телефона и код подтверждения из Telegram.

## ⚙️ Конфигурация

### Настройки Telegram
| Параметр | Описание | Пример |
|----------|----------|---------|
| `api_id` | API ID из my.telegram.org | 1234567 |
| `api_hash` | API Hash из my.telegram.org | abcdef123456 |

### Настройки репоста
| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `period` | Период сканирования (часы) | 24 |
| `min_views` | Минимальные просмотры (0 = отключено) | 0 |
| `min_reactions` | Минимальные реакции (0 = отключено) | 0 |
| `min_comments` | Минимальные комментарии (0 = отключено) | 0 |
| `min_delay` | Минимальная задержка между постами (сек) | 5.0 |
| `max_delay` | Максимальная задержка между постами (сек) | 15.0 |

### Настройки нейросетей
| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `api_key` | Ключ от io.net IO Intelligence | - |
| `models` | Список моделей через запятую | см. пример |
| `temp` | Температура (0.0-2.0) | 0.65 |
| `top_p` | Nucleus sampling (0.7-0.95) | 0.9 |
| `max_tokens` | Максимальное количество токенов | 512 |

## 📊 Поддерживаемые модели

Доступные модели (настраиваются в config.ini):
- `deepseek-ai/DeepSeek-R1-0528`
- `meta-llama/Llama-4-Maverick-17B-128E-Instruct-FP8`
- `openai/gpt-oss-120b`
- `Qwen/Qwen3-Next-80B-A3B-Instruct`
- `meta-llama/Llama-3.3-70B-Instruct`
- и другие...

## 🗂️ Структура проекта

```
tg_reposter/
├── main.py              # Основной скрипт
├── telegram_handler.py  # Обработчик Telegram
├── settings.py          # Конфигурация
├── history_manager.py   # Управление историей
├── log_setup.py         # Настройка логирования
├── config.ini           # Настройки (создается)
├── sent_posts.json      # История отправленных постов
├── requirements.txt     # Зависимости
└── README.md           # Документация
```

## 🔧 Управление историей

История отправленных постов хранится в `sent_posts.json`. Для очистки:

```json
{
  "last_updated": "2026-01-12T18:38:51.040897",
  "sent_posts": []
}
```

Или удалите файл `sent_posts.json`.

## 📈 Статистика

После выполнения выводится статистика:
- Количество обработанных/отправленных/пропущенных постов
- Потрачено токенов
- Использованные модели
- Активность по каналам

## 📝 Логирование

Логи выводятся с цветовым кодированием:
- ✅ Успешно
- ⚠️ Предупреждение
- ❌ Ошибка
- 🔄 В процессе

## 🔄 Планировщик

Для автоматического запуска можно использовать cron (Linux) или Task Scheduler (Windows):

```bash
# Запуск каждые 6 часов
0 */6 * * * cd /path/to/tg_reposter && python main.py
```
